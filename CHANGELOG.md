# Changelog

## 0.1.4

- Added support for `go.mod` files with inline module size annotations.
- Implemented `go.mod` parsing for `require (...)` blocks and single-line `require` entries.
- Added Go module size lookup via `proxy.golang.org` module archive size.
- Updated activation events and documentation to include `go.mod`.
